import SwiftUI

@main
struct NovaApp: App {
    init() {
        // noop - songs loaded from server when imported
    }

    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
